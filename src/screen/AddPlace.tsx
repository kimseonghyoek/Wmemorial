import React from 'react';
import { SafeAreaView, Text } from "react-native";

function AddPlace(): React.JSX.Element {
  return (
    <SafeAreaView>
      <Text>장소 추가 Screens</Text>
    </SafeAreaView>
  );
}

export default AddPlace;
